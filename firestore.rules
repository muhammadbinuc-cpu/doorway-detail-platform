rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /jobs/{jobId} {
      // Allow public to create new quotes (Lead Generation)
      allow create: if true;
      
      // Only authenticated staff can see, update, or delete jobs
      allow read, update, delete: if request.auth != null;
    }
  }
}
